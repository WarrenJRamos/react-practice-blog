import React from "react";
import classes from "../../styles/components/Layout/Header.module.css";

const Header = () => {
    return (
        <header className={classes.header}>
            <h1>Warren's Blog App</h1>
        </header>
    );
};

export default Header;
