import React from 'react';

// Convert this into a modal after using React portals
const UpdatePost = () => {
    return (
        <div>
            
        </div>
    );
}

export default UpdatePost;
